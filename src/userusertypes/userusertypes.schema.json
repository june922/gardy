{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "Userusertypes",
    "description": "The User  user Types schema",
    "required": [
        "user_type_id"
    ],
    "additionalProperties": true,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the user type."
        },
        "user_id": {
            "$id": "#/properties/user_id",
            "type": "integer",
            "title": "The ID of the user."
        },
        "user_type_id": {
            "$id": "#/properties/user_type_id",
            "type": "integer",
            "title": "The ID of the user type."
        },
        
        "created_by": {
            "$id": "#/properties/created_by",
            "type": "integer",
            "title": "Speciality created by."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date of the user type."
        },
        "updated_at": {
            "$id": "#/properties/updated_at",
            "type": "string",
            "title": "The date the user type was last updated."
        }
    }
}