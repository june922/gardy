{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "notification_type",
    "description": "The notification_type",
    "required": [
        "name"
    ],
    "additionalProperties": true,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the notification_type."
        },
        "name": {
            "$id": "#/properties/name",
            "type": "string",
            "title": "The notification_type."
            
        },
        "created_by": {
            "$id": "#/properties/created_by",
            "type": "integer",
            "title": "notification_type."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date of the notification_type."
        },
        "updated_at": {
            "$id": "#/properties/updated_at",
            "type": "string",
            "title": "The date the notification_typepdated."
        }
    }
}