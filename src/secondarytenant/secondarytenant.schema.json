{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "secondarytenants",
    "description": "The secondary tenant schema",
    "required": [
        "user_id"
    ],
    "additionalProperties": true,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the secondary tenant."
        },
        "tenant_id": {
            "$id": "#/properties/tenant_id",
            "type": "integer"   ,
            "title": "References primary tenant id .",
            "description": "This is the primary tenant id that this secondary tenant belongs to."
        },
        "user_id": {
            "$id": "#/properties/user_id",
            "type": "integer",
            "title": "The id of the user.",
            "description": "This is the user id of the secondary tenant."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date of the user role."
        
        }
    }
}