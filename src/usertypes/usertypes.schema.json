{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "User Types",
    "description": "The User Types schema",
    "required": [
        "name"
    ],
    "additionalProperties": true,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the user type."
        },
        "name": {
            "$id": "#/properties/name",
            "type": "string",
            "title": "The user type name.",
            "examples": [
                "Test"
            ]
        },
        "created_by": {
            "$id": "#/properties/created_by",
            "type": "integer",
            "title": "Speciality created by."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date of the user type."
        },
        "updated_at": {
            "$id": "#/properties/updated_at",
            "type": "string",
            "title": "The date the user type was last updated."
        }
    }
}