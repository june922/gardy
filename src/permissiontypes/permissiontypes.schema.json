{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "Permission Types",
    "description": "The Permission Types schema",
    "required": [
        "name"
    ],
    "additionalProperties": true,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the Permission type."
        },
        "name": {
            "$id": "#/properties/name",
            "type": "string",
            "title": "The user type name.",
            "examples": [
                "Test"
            ]
        },
        "created_by": {
            "$id": "#/properties/created_by",
            "type": "integer",
            "title": " created by."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date of the permission type."
        },
        "updated_at": {
            "$id": "#/properties/updated_at",
            "type": "string",
            "title": "The date the permission type was last updated."
        }
    }
}