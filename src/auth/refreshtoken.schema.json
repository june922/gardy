{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "title": "refresh token",
    "description": "The refresh token schema",
    "required": [
        
    ],
    "additionalProperties": true,
    "properties": {
        "id": {
            "$id": "#/properties/id",
            "type": "integer",
            "title": "The database ID of the  token."
        },
        "refresh_token": {
            "$id": "#/properties/refresh_token",
            "type": "string",
            "title": "The  token name."
        },
        "created_at": {
            "$id": "#/properties/created_at",
            "type": "string",
            "title": "The creation date of the  token."
        },
        "expiry_date": {
            "$id": "#/properties/expiry_date",
            "type": "string",
            "title": "The expiry_date date of the  token."
        },
        "updated_at": {
            "$id": "#/properties/updated_at",
            "type": "string",
            "title": "The date the  token was last updated."
        }
    }
}